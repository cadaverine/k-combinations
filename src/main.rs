fn get_k_combinations_number(n: u64, k: u64) -> u64 {
    assert!(n >= k);

    if n == k {
        return 1;
    }

    let mut numeratorArray = arr[1..k];

}


fn k_combinations_generate(n: u64, k: u64) -> u64 {

}


fn main() {
    print!("{}", (1..20));
}
